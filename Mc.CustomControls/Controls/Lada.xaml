<UserControl x:Class="Mc.CustomControls.Controls.Lada"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="50" Width="50">
    <Grid>
    <Canvas  Name="canvas" Height="{Binding ElementName=root, Path=Height}"  
            Width="{Binding ElementName=root, Path=Width}">
        <Rectangle Name="Close"  Fill="HotPink"
            Width="10" 
                   Height="{Binding ElementName=canvas,Path=ActualHeight}">
        </Rectangle>
            <Rectangle Name="Open"  Fill="HotPink"
            Width="{Binding ElementName=canvas,Path=ActualWidth}" 
                   Height="10">
            </Rectangle>
        </Canvas>
    </Grid>
</UserControl>
